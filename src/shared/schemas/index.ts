import { SigninFormData, signinSchema } from "./signin-schema";
import { SignupFormData, signupSchema } from "./signup-schema";

export type { SigninFormData, SignupFormData };
export { signinSchema, signupSchema };
